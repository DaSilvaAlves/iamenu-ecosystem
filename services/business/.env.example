# Business API - Environment Variables
# Copy to .env and fill in your local values
# NEVER commit .env to git - contains sensitive information

# ===================================
# Server Configuration
# ===================================
PORT=3004
NODE_ENV="development"
LOG_LEVEL="debug"

# ===================================
# Database (PostgreSQL with business schema)
# ===================================
# Use schema=business for multi-schema isolation
# Change from SQLite to PostgreSQL for consistency
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/iamenu?schema=business"

# Alternative for development (local SQLite):
# DATABASE_URL="file:./dev.db"

# ===================================
# Authentication (Shared across all services)
# ===================================
# IMPORTANT: Must match JWT_SECRET in all services
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-random-jwt-secret-minimum-32-chars"

# ===================================
# CORS Configuration
# ===================================
# Frontend URL(s) - can be comma-separated for multiple origins
CORS_ORIGIN="http://localhost:5173"

# ===================================
# API Configuration
# ===================================
API_BASE="/api/v1/business"
