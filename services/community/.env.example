# Community API - Environment Variables
# Copy to .env and fill in your local values
# NEVER commit .env to git - contains sensitive information

# ===================================
# Server Configuration
# ===================================
PORT=3001
NODE_ENV="development"
LOG_LEVEL="debug"

# ===================================
# Database (PostgreSQL with community schema)
# ===================================
# Use schema=community for multi-schema isolation
# Default: postgres user with password 'postgres' on localhost
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/iamenu?schema=community"

# ===================================
# Authentication (Shared across all services)
# ===================================
# IMPORTANT: Must match JWT_SECRET in all services
# Generate a random secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-random-jwt-secret-minimum-32-chars"

# ===================================
# CORS Configuration
# ===================================
# Frontend URL
CORS_ORIGIN="http://localhost:5173"

# ===================================
# Optional: OpenAI (for AI features)
# ===================================
# OPENAI_API_KEY="sk-proj-xxxxxxxxxxxxxxxxxxxx"
